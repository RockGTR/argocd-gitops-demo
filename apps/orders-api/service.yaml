# Orders API — Service
apiVersion: v1
kind: Service
metadata:
  name: orders-api
  labels:
    app: orders-api
spec:
  selector:
    app: orders-api
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
